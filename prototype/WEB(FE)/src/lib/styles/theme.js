import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#000F04',
    },
    secondary: {
      main: '#073113',
    },
  },
  // shadows: 0,
});

export default theme;
