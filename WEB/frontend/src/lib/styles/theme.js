import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    status: {
    
    },
    palette: {
        primary: {
            main: '#000F04',
        },
        secondary: {
            main: '#073113',
        },
    }
});

export default theme;
