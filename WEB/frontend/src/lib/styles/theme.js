import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    status: {
    
    },
    palette: {
        primary: {
            main: '#073113',
            light: '#335b39',
            dark: '#000c00',
        },
        neutral: {
            
        },
    },
});

export default theme;